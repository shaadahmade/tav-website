import React from 'react';
import { ServicePage } from './services/ServicePage';
import { graphicDesignData } from './services/serviceData';

export const GraphicDesignService: React.FC = () => <ServicePage data={graphicDesignData} />;
